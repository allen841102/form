#https://docs.amazonaws.cn/en_us/elasticbeanstalk/latest/dg/create_deploy_PHP.container.html
#composer_options 是 PHP platform 特有的選項，設定 --no-dev 會加上去，在解壓縮後就立刻安裝相依套件。如果目錄中有 vendor ，則不會執行 composer install。
option_settings:
  aws:elasticbeanstalk:container:php:phpini:
    document_root: /public
    memory_limit: 128M
    zlib.output_compression: "Off"
    allow_url_fopen: "On"
    display_errors: "Off"
    max_execution_time: 30
    composer_options: --no-dev
